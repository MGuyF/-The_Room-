{
  "name": "project_test",
  "version": "1.0.0",
  "description": "This repository is about a project-test in order to check if we are able to make a team of programmer",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "mguyf_artwork",
  "license": "ISC",
  "dependencies": {
    "normalize.css": "^8.0.1"
  }
}
